#!/usr/bin/env python3
print('''Instead of hardcoding python interpreter paths, use the environment variable (provided you have access to it).
So if ever python moves to a new location, just update the env variable, not all scripts :)''')
